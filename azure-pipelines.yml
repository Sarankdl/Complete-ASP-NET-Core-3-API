trigger:
- main
pool:
  vmImage: 'ubuntu-latest'
variables:
  buildConfiguration: 'Release'
steps:
- task: UseDotNet@2
- script: dotnet build Complete-ASP-NET-Core-3-API/CommandAPISolution/CommandAPISolution.sln --configuration $(buildConfiguration)
  displayName: 'dotnet build $(buildConfiguration)'